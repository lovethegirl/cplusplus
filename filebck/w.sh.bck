#!/bin/bash

for file in `ls *.sh`
do
    echo ${file} is file path \!
done

your_name="tom"
echo ${your_name}
your_name="ljw"
echo ${your_name}

path="w.sh"
for process in `ps -aux | grep ${path}`
do
    if [ ${process}=${path} ]
    then
        echo "this is ${process}"
    fi
done

